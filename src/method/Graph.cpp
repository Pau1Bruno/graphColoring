#include "Graph.h"

// All methods are defined inline in Graph.h, so nothing to compile here.
// This translation unit only ensures that a .o file is produced when you
// list Graph.cpp in your build command / CMakeLists.